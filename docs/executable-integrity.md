# Executable Integrity

Phase 7 requires trusted product ID, version, relative artifact path, and SHA-256 metadata. Missing, replaced, escaped, or modified artifacts fail closed. Platform signing verification is not claimed.
