# Process Lifecycle

Execution returns typed launch states, tracks active processes by product, prevents duplicate concurrent launches, and supports explicit termination. Process records contain no credentials.
