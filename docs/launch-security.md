# Launch Security

Launch is separate from authorization. SQLite and manifests are not trust anchors; only a current Phase 6 decision and matching trusted artifact metadata permit process creation. No shell or secret-bearing arguments are used.
